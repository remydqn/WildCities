

  <input name="criteria[wild]" type="range" min="0" max="5">
  <%= f.button :submit %>

<%= f.association :criterias, as: :check_boxes %>

<%= Criteria.all.each do |criteria| %>
<span class="checkbox">
  <label for="patch_criteria_ids1">
    <input class="check_boxes optional" type="checkbox" value="1" name="patch[criteria_ids1]" id="patch_criteria_ids1"%>
    Drink
</label>
</span>
<%end%>


<%= simple_form_for @criteria, url: criterias_path, method: :patch do |f| %>

  <%= f.input :dance, label: 'dance'  %>
  <%= f.input :culture, label: 'culture'  %>

<% end %>
